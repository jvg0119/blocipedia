<div class="col-md-12">
<!-- <h1>My Wikis</h1> -->

<div class="container-fluid">
  <h2><%= current_user.name %>'s Private Wikis</h2>
  <p></p>            
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Title</th>
        <th></th>
        <th></th>
      </tr>
    </thead>

    	<%#= render @wikis %>
      <%= render partial: 'wikis/wiki', locals: { wiki: @private_wikis }, collection: @private_wikis %>

  </table>

<br />

	<%= link_to "Create New Wiki", new_wiki_path, class: "btn btn-success" if policy(Wiki.new).create? %>
</div>

<%= will_paginate @private_wikis %>

<br />
